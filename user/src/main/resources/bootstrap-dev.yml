#设置服务发现

spring:
  application:
    name: user-dev
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        prefix: shopping
        file-extension: yml
